{# templates/product/show.html.twig #} {% extends 'base.html.twig' %} {% block
title %}Product Details{% endblock %} {% block body %}
<h1>Product Details</h1>

<table class="table table-bordered">
  <tr>
    <th>ID</th>
    <td>{{ product.id }}</td>
  </tr>
  <tr>
    <th>Name</th>
    <td>{{ product.name }}</td>
  </tr>
  <tr>
    <th>Thumbnail</th>
    <td>{{ product.thumbnail }}</td>
  </tr>
  <tr>
    <th>Description</th>
    <td>{{ product.description }}</td>
  </tr>
  <tr>
    <th>Stock</th>
    <td>{{ product.stock }}</td>
  </tr>
  <tr>
    <th>Status</th>
    <td>{{ product.status ? "Active" : "Inactive" }}</td>
  </tr>
</table>

<a href="{{ path('product_index') }}" class="btn btn-secondary">Back to List</a>
<a href="{{ path('product_edit', { id: product.id }) }}" class="btn btn-warning"
  >Edit</a
>

{{ include("product/_delete_form.html.twig") }}
{% endblock %}
